实现多线程几种方式？
两种 继承Thread类  和  实现Runnable接口

同步几种方式？
同步代码块，同步关键字声明的方法，同步锁加锁解锁

Sleep和Wait区别
Sleep休眠，能释放时间片，不会丢失任何显示器的所有权；wait等待状态，等待notify和notifyAll唤醒，wait也能释放时间片，但是会丢失显示器所有权

线程池？
预先创建线程，线程还没任务到来之前就创建一定数量线程放入空闲队列，然后对这些资源进行复用，减少频繁创建和销毁对象