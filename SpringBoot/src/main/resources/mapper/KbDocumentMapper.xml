<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.shmilyqjj.springboot.mappers.KbDocumentMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO test.kb_document (original_filename, stored_filename, content_type)
        VALUES (#{originalFilename}, #{storedFilename}, #{contentType})
    </insert>

    <delete id="deleteById" parameterType="long">
        DELETE FROM test.kb_document WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultType="top.shmilyqjj.springboot.models.entity.KbDocument">
        SELECT id, original_filename AS originalFilename, stored_filename AS storedFilename,
               content_type AS contentType, created_at AS createdAt
        FROM test.kb_document WHERE id = #{id}
    </select>

    <select id="listAll" resultType="top.shmilyqjj.springboot.models.entity.KbDocument">
        SELECT id, original_filename AS originalFilename, stored_filename AS storedFilename,
               content_type AS contentType, created_at AS createdAt
        FROM test.kb_document ORDER BY id DESC
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM test.kb_document
    </select>
</mapper>
